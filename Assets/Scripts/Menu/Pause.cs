using UnityEngine;
using System.Collections;

public class Pause : MonoBehaviour {

	private Vector3 positionsouris;
	private Vector3 positionBoule;

	// Use this for initialization
	void Start () {

	}
	
	// Update is called once per frame
	void Update () {
		//this.transform.localPosition = new Vector3(Input.mousePosition.x, Input.mousePosition.y, transform.position.z);
	
	}
}
